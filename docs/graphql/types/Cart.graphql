type Cart { 
    id: ID!
    userId: ID!
    items: [CartItem!]!
    total: Decimal!
    createdAt: DateTime!
    updatedAt: DateTime!
}

type CartItem{
    id: ID!
    product: Product!
    quantity: Int!
    subTotal: Decimal!
} 